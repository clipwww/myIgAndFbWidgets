{
  "name": "my-ig-widgets",
  "devDependencies": {
    "babel-polyfill": "^6.26.0",
    "parcel": "^1.11.0",
    "sass": "^1.16.1"
  },
  "keywords": [],
  "scripts": {
    "prestart": "rm -rf dist",
    "start": "parcel index.html",
    "prebuild": "rm -rf dist",
    "build": "parcel build index.html",
    "prestart:window": "if exist dist RMDIR dist /s /q && if exist mkdir dist /s /q",
    "start:window": "parcel index.html",
    "build:window": "npm run prestart:window && parcel build index.html"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "axios-jsonp": "^1.0.2",
    "vue": "^2.5.22"
  }
}
